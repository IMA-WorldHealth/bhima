/* global inject */

describe('StockService', StockServiceTests);

function StockServiceTests() {
  // define modules
  beforeEach(module(
    'bhima.services',
    ''
  ));

  // inject dependencies
  beforeEach(inject(() => {

  }));
}
