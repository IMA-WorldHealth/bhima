version: v1.0
name: Run in Docker
agent:
  machine:
    type: e1-standard-2
  containers:
    - name: main
      image: 'semaphoreci/node:10' # Your primary container where commands are run
    - name: database
      image: 'postgres:11'

blocks:
  - name: Hello World
    task:
      jobs:
        - name: Test job
          commands:
            - checkout # Check out the code
            - echo "Hello from my 'main' container"
